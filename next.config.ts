import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // Imagens locais em /public não precisam de configuração adicional no Next.js.
  // Para permitir imagens de domínios externos, adicione remotePatterns aqui.
  // Exemplo:
  // images: {
  //   remotePatterns: [
  //     { protocol: "https", hostname: "exemplo.com" },
  //   ],
  // },
};

export default nextConfig;
